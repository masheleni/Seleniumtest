pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // sh 'cd to dir'
                sh 'mvn clean test'
        
            }
        }
       
    }
}

